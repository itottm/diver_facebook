<div class="container">
  <div class="wrapper col-md-8 col-md-offset-2 col-sm-10 col-sm-offset-1">
    <p><%= notice %></p>

    <%= render 'form' %>

    <div id="topics">
      <ul class="page">
        <%= render @topics %>
      </ul>
    </div>

    <%= paginate @topics, remote: true  %>

  </div>
</div>